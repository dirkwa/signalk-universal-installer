# Hardware passthrough

Placeholder. Real content lands in Phase 7 (detection) and Phase 10 (toggle UI).

Will cover:

- USB serial (`/dev/serial/by-id/*`).
- SocketCAN interfaces.
- Bluetooth (DBus passthrough).
- Raspberry Pi GPIO.
- The Hardware tab in the Updater Console.
- Group memberships (`dialout`, `gpio`, `netdev`) required for rootless Podman device access.
