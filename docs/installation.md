# Installation

Placeholder. Real content lands in Phase 7 (Linux) and Phase 8 (macOS, Windows/WSL2).

Will cover:

- Supported platforms (Debian 13, Ubuntu 24/26, Raspbian/Pi OS Bookworm+, macOS via Podman Machine, Windows via WSL2).
- Pre-flight requirements (RAM, disk, cgroups v2, Podman ≥ 4.4).
- The `curl … | bash` one-liner.
- What the installer writes to your home directory.
- How to reach the Updater Console (`:3003`) and Doctor Console (`:3004`).
